TASK: Deep scrutiny of C_Order → compilation logic for SH and DX (both when running as singular/EN-BLOC and then multi/EXPLODE). Prove the pipeline follows first principles with no cheating,                                                                                                     
  no shortcuts, no rule bypass.                             

  PRIME RULE: EXTRACT, DON'T IMAGINE. Query the database. Read the code. Never assume.

  CONTEXT:
  - SH (Ifc4_SampleHouse): 56 elements, RESIDENTIAL, doc_sub_type=SH, C_DocType_ID=RE_SH
  - DX (Ifc2x3_Duplex): 1099 elements, RESIDENTIAL, doc_sub_type=DX, C_DocType_ID=RE_DX
  - Both are EXTRACTED (provenance), not GENERATIVE. Their geometry comes from IFC files
    stored in component_library.db, not invented by the compiler.
  - The compilation pipeline is in CompilationPipeline.java (9 stages)
  - {PREFIX}_BOM.db is the read-only dictionary. output.db is created fresh each run.

  INVESTIGATE THESE QUESTIONS — read actual code, not just Javadoc:

  1. C_ORDER LOADING: How does BuildingRegistry.java load c_order rows? Trace the SQL.
     Does it filter by is_active? Does it load C_DocType_ID? Does cbpartner (now the
     doc_sub_type value) flow correctly into the pipeline context?

  2. EN-BLOC vs EXPLODE DECISION: Where in the pipeline is the singular vs multi
     decision made? Is it based on DocSubType matching the UNIT BOM's doc_sub_type?
     Read CompilationPipeline.java CompileStage — trace the "Look up UNIT BOM" logic:
     - How does it find the UNIT BOM? (SQL: doc_sub_type = ? AND bom_category = 'UN')
     - What happens when it finds one (SH case)?
     - What happens when it doesn't (ST case)?
     - DX has a UNIT BOM too — verify it follows the same path as SH.

  3. CO_EMPTYSPACE CREATION: The CompileStage creates CO_EmptySpace header + lines.
     - Level-0 line: the full building AABB. What BOM ID goes here?
     - Level-1 lines: per-storey decomposition. Where do storey dimensions come from?
     - Does SH get 1 storey line? Does DX get 2 (or more)?
     - Verify by running: sqlite3 output/Ifc4_SampleHouse.db "SELECT * FROM co_empty_space_line ORDER BY bom_level, bom_line_seq"
     - Same for: sqlite3 output/Ifc2x3_Duplex.db "SELECT * FROM co_empty_space_line ORDER BY bom_level, bom_line_seq"

  4. ELEMENT SOURCING: Where do the 56 SH elements and 1099 DX elements actually come
     from? Trace BuildingWriter.java — the writeElements / writeStorey / writeFurniture
     methods. Do they read from component_library.db (extracted IFC geometry) or do they
     invent anything? Check:
     - Are element GUIDs preserved from reference?
     - Are bounding boxes (elements_rtree) copied from reference or recomputed?
     - Is any element generated that doesn't exist in the reference DB?

  5. DSL vs BOM PATH: The dsl_content in c_order contains the building DSL script.
     - Does the DSL path (ParseStage → CompileStage) create elements directly?
     - Or does it only establish spatial structure, with elements coming from BOM/reference?
     - Is there any code path that creates IfcWall/IfcDoor/IfcWindow etc from DSL alone
       without a component_library.db lookup?
     - Check StoreyCompiler, WallCompiler, RoomCompiler if they exist.

  6. REFERENCE DB SHADOW VALIDATION: BuildingRegistryTest checks the output against
     reference DBs. Read the shadow validation code. Does it compare element-by-element?
     What exactly does it verify? Is there a path where SH or DX could pass the gate
     with invented elements that don't exist in reference?

  7. BOM TREE INTEGRITY FOR DX: DX is the complex case — two mirrored half-units.
     - What is the UNIT BOM for DX? (Query: sqlite3 library/{PREFIX}_BOM.db "SELECT * FROM m_bom WHERE doc_sub_type='DX' AND bom_category='UN'")
     - How many m_bom_line children does it have? What are the storey BOMs?
     - Does the pipeline actually walk this tree, or does it use a shortcut?
     - The 1099 elements — verify they all trace to component_library.db products.

  8. TEMPLATE STAGE BYPASS: TemplateStage is "ST mode only" (skipped for SH/DX).
     Verify this skip logic. Read the shouldSkip() method. Confirm SH and DX never
     enter the template path. If they did, what would break?

  9. PLACEMENT PROVER: PlacementProver runs as Stage 9 for SH and DX (skipped for ST).
     What proofs does it check? Does it verify that every compiled element exists in
     the reference? That no extra elements were added? Read the critical violations
     logic.

  10. WRITE STAGE ORDER: WriteStage copies c_order + c_orderline to output.db.
      - Does it copy C_DocType_ID? (Check the INSERT statement)
      - Does it faithfully copy all columns or does it transform/invent anything?
      - Verify the parameter count matches the column count (off-by-one = silent data corruption)

  FOR EACH QUESTION:
  - Cite exact file:line_number
  - Show the actual SQL or Java logic
  - Verdict: CLEAN (follows rules), SHORTCUT (explain what's bypassed), or
    CHEATING (invents data not in source)

  OUTPUT: A verdict table for all 10 questions + overall assessment:
  Does the SH/DX compilation pipeline faithfully transform {PREFIX}_BOM.db + component_library.db
  into output.db without inventing, skipping, or bypassing any first-principle rules?

  FILES TO READ:
  - DAGCompiler/src/main/java/com/bim/compiler/dsl/CompilationPipeline.java (ALL of it)
  - DAGCompiler/src/main/java/com/bim/compiler/dsl/BuildingRegistry.java
  - DAGCompiler/src/main/java/com/bim/compiler/dsl/BuildingWriter.java
  - DAGCompiler/src/main/java/com/bim/compiler/dsl/BuildingSpecs.java
  - DAGCompiler/src/main/java/com/bim/compiler/validation/PlacementProver.java
  - DAGCompiler/src/test/java/com/bim/compiler/contract/BuildingRegistryTest.java
  - library/{PREFIX}_BOM.db (query directly for DX/SH BOM structure)
  - output/Ifc4_SampleHouse.db and output/Ifc2x3_Duplex.db (query for actual output)

  This prompt hits 10 specific code paths where cheating could hide. The critical ones are #4 (element sourcing — do elements come from IFC or invention?), #5 (does DSL create elements
   directly?), and #7 (does DX actually walk the BOM tree or shortcut?). Each demands file:line citations, not hand-waving.

