# PILLS_TO_GLASSBOWL_CONSOLIDATE.md — fold Glassbowl + Gravity loose buttons into the ⋯ pill registry (SPEC)

# ⚠ DO NOT REMOVE
> **Scope:** Glassbowl (`glassbowl.html`, GENERATED by `scripts/system_explorer.js`) + Gravity
> (`glassbowl_gravity.html`, hand-authored) currently carry HAND-ROLLED button bars. Move EVERY
> loose control into the SAME `pill_builder.js` ⋯ registry already used by `erp.html`/`idempiere.html`
> — incl. a **Home pill → erp.html** so you can always return. "No controls outside the pill"
> (§PILL-REGISTRY). Clean Lucide icons only (icons.js). READ THE LOG after every run. Honour until DONE.
> **User ask (2026-06-10):** "Glassbowl and Gravity should have all its buttons (edit bubble / need
> help) in pill registry which has home also so it can return back to ERP.html. Check also any other
> loose feature/button to move to the ⋯ pill."

## The registry (reuse — do NOT write a second framework)
`pill_builder.js` (`window.PillBuilder`) + `icons.js` (`window.ICONS`) are already in `erp/`. Pattern =
`idmp_pills.js` + `pills_idmp.json` + `window.IdmpPillActions`: a DATA manifest (icons by id), a binding
module that maps pill id → the REAL existing host handler, then `PillBuilder` mounts the ⋯ dock with
collapse + per-user {order,hidden} persistence. Sibling manifest per surface (action sets differ).

## Loose controls to fold (audit)
**Glassbowl** (`system_explorer.js` VIEWER_JS / HTML):
- `.ctl` bar: `traceBtn` (▸ Trace) · `untangleBtn` (✦ Untangle) · `muteBtn` (🔊) · `resetBtn` (⤢ Reset) · `panelToggle` (⟩)
- `aboutBtn` (ⓘ how-to) · `qrbtn` (▢ Scan QR) · the help_overlay.js floating **Need Help?** checkbox (`#needHelpCk`/`#needHelpWrap`)
- **edit bubble** — the CRUD edit affordance (crud_overlay.js) reached from the dossier; surface as an `edit` pill
- **NEW: `home` pill → `erp.html`**
**Gravity** (`glassbowl_gravity.html`):
- lens chips (Partner/Month/Status/Type) + measure chips (count/value) — KEEP as the in-canvas lens selector (NOT pills; they are the primary lens control), but evaluate moving `undoBtn`/`redoBtn` + the record-strip `Edit` into pills.
- `undoBtn` (↶) · `redoBtn` (↷) · strip `✎ Edit` (disabled stub) · **NEW: `home` pill → erp.html** + **Help/ShowMe**.

## Proposed manifest `pills_glassbowl.json` (sibling; bind in `glassbowl_pills.js` → `window.GlassbowlPillActions`)
home(houseIcon→erp.html) · trace · untangle · reset · mute(toggle) · panel(toggle) · edit · qr · showme(help, toggles #needHelpCk) · about.
Gravity reuses the same binding module with its own action set (or a `pills_gravity.json`): home · undo · redo · edit · showme.

## Witnesses
- **W-GB-PILLS** — `§GB-PILLS built=<n> handAuthored=0`: PillBuilder renders the dock; the OLD `.ctl`/loose
  buttons are GONE from the DOM; each pill id resolves a real handler; the ⋯ collapse + persistence work.
- **W-HOME** — tapping `home` navigates to `erp.html` (assert `location` change / a `§GB-HOME → erp.html` log).
- **W-GRV-PILLS** — same for gravity.
- Regression: the existing `§GLASSBOWL-WIRING` checks for trace/untangle/reset/mute still pass (now driven via pills).

## Constraint — Glassbowl is GENERATED
`scripts/system_explorer.js` emits `glassbowl.html`. In THIS env the generator ERRORS at line ~244
(`diff_oracle.js` `no such column: id` — a stale/changed erp DB) so it can't be re-run to regenerate.
Until that's fixed, apply EVERY glassbowl change to BOTH (a) the generator strings (source of truth) AND
(b) the deployed `erp/glassbowl.html` by hand — the SAME double-edit used for the diag-removal + SW fix
(PR #233). Gravity is hand-authored (single edit). New files (`pills_glassbowl.json`, `glassbowl_pills.js`)
live in `erp/` and are loaded by both the generated page and gravity.

## Discipline
Additive registry mount; remove the loose bars only AFTER the pills prove out. Clean Lucide icons (icons.js)
— no unicode glyphs. Test headless (§-log first) on a served worktree; bump `sw.js` CACHE_VERSION + precache
the 2 new assets. NO blind copy of build/erp → bim-ootb (build/erp glassbowl is BEHIND deployed — it lacks
whole_history/period-close). Deploy = edit in a `/tmp/wt-*` worktree → PR → auto-merge.
