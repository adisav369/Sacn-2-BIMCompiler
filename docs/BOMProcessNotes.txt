1. AD_Room_Slot maybe deprecated by BOM_Category. WH_ should be CO signifying Construction Order. Thus CO_EmptySpace/CO_EmptySpaceLine
2. I wonder whether after our previous long discussion, covering main models, we continue into the process itself. 
3. I like to propose that all BOM information be moved to a separate BOM.db leaving component library db solely LOD mesh object info with basic top/front bbox, weight values.
4. BOM.db is the relationships construct of a combination of the LODs in the component library. Each construct is the BOM as described before containing rich info such as space/orientation.
5. When a Work or Construction Order is raised, ie. C_Order (! just nice). So instead of DSL or BIM we call it outright C_Order for proximity to iDempiere lingo.
6. C_Order has BOM_Vendor 'DX' Duplex, and a C_Orderline tab, a CO_EmptySpace tab
7. C_Orderline has a BOM tab with several generations of BOM Lines layers.
8. The CO_EmptySpace holds construction site information such as real world location and origin spot and AABB of whole intended construction space. At start of project IsAvailable flag is true. When a line appears in BOM tab, it is set to false. Its first sub detail tab CO_EmptySpaceLine just points to the BOM tab first record. We thus kinda deviate from earlier definition of EmptySpace for time being to see how this plays out.
9. There are first level BOM Residential records in Component Library ie. 'DX' Duplex, 'SH' SampleHouse, 'TB' and 'TE' as discussed before. We select 'DX' for this example here.
10. Process button (DAGCompiler or run.sh) will explode the BOM tab next detail tab - BOMLine children (Roof,L1,L2,Floor) also discussed before.
11. The BOM.db rich info  transferred are translated BBOx layer by layer that fills up EmptySpace AABB space. 
12. Reason why they all fit as the original extracted Duplex is because there is only single record for each layer of BOM. Ie, Roof there is only one roof bom. L1 there is only one L1, and L2 and floor. In L1 there is only one of each child room and each room only one set of items until the whole construction is as original. 
13. In full topology variant mode if the EmptySpace is larger, it will look for any other bigger variant. Likewise smaller also do so. Thus for TBLKTN it cannot fulfil its construction unless Topology already exists for its unit and floor level. That is why TopologyMaker has to create those smaller unit and floor. Unit can be another 'SH' BOMCategory.
14. For furniture, since BOM.db already has all sorts of smaller constructs of furnishing, there is no worry for TBKLTN to get all things done. Its RoofPorch and outer perimiter also done. What is left is a mesh floor.
15. Now the recurring placement errors which will now increase since we already include unit and floor into the stack, it will replicate what we faced at the furniture level.
16. The stubborn and dumb issue is we did not translate its BOM construct correctly to the EmptySpaceLine.
7. This is where it is clear what EmptySpaceLine is for. It repeats each expanded BOMLine and keep their before/next spatial connecting info and orientation. Thus the BOMLine tab is unchanged, all the BOM.db copied ad verbatim. Acts as good reference during calculation without needing to refer to BOM.db as what is in OrderLine.BOM tab is selection and won't confuse the scope. Translation is assisted by the EmptySpaceline by holding its respective info described. It can also hold the MEP info so to separate and not corrupt the BOM leaf items. There can be further EmptySpacelines each holding a particularly spatial reference for a particular function such as later in 7D IoT equipment. Thus it is important placeholder function.

Wonder if u can figure out the complete picture of where each tiny info should sit in before exporting to output DB its IFC blended data. Yes this is evolving but we are still in our early design stage to nail an elegant working model that satisfy the construction model. Write this as ConstructionAsERP.md

Your pattern recognition is the needed strength in this, ability to scan across wide and fast and finding as much matching aspects such as the Acct-Schema as the MEP cross spatial objects is impressive to me.

Now, towards end of last year, we together contributed to IfcOpenShell an addon in github.com/red1oon/ the migrated IFC to extracted FederatedModel database, which allows scalability for the 51k elements Terminal complex which simply crashes in Bonsai is now so smooth. Consequentially, you able to recognise the 4D till 7D, that u sailed thru even added in Coloring and Natural Language query. So emboldened, i was not satisfied with this been just an Autodesk downstream handler, saving the users from investing further in licensing over disprate apps, i tried to tackle replacing Autodesk itself which is too big a task. Then i hit on the idea of 2D to 3D as a flanking move but AI has a blindspot corelating geometry as it is not its type of LLM training. After months of frustration i hit upon an abstract extrapolation which is why not go one step back to 1D which i called Intent, and thus the DAG Compiler DSL concept was born. 

But again hitting AI blindspots, i came upon the Rosetta Stone concept been how linguists breakthru something that has never been learned, is to establish a dictionary. Now that is in process though excruciating as again i have to introduce concepts i been stating above including Java patterns i know from iDempiere which are more powerful.

Once this is done, there be just extrapolating topologies of objects and their BOM constructs into a huge rich library that users can construct almost anything. Then a GUI editor to manage those ontology orthagonally.

Here u can tell me if the AD direction is powerful as the underlying engine to manage, maintain, scale and craft a feature rich experience.

Write something about this big picture roadmap in the doc.
