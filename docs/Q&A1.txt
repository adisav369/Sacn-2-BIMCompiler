Now, the questions. I read through PROGRESS.md, MEMORY.md, ConstructionAsERP.md references, and all the architecture notes. Here are the model areas where I see ambiguity, tension, 
  or missing design decisions — these are the spots where your vision would sharpen everything:                                                                                         
                                                            
  1. EN-BLOC vs EXPLODE: When exactly does the compiler choose?
** When C_Order.C_BPartner is SH or DX same as M_BOM.C_BPartner.  **
  The current description says "C_BPartner match + exactly one BOM = EN-BLOC, no match = EXPLODE walk." But the implementation path is still TODO. Concrete questions:
** Since we only have SH/DX complete BOM stack, they thus fit perfectly and thus the singularity can happen. **
  - Who triggers the decision? Is it CompilationPipeline at the ESLine generation stage, or earlier at BOM resolution?
  ** CompilationPipeline supposed to generate C_OorderLines (if no further user specs. User spec DSL in OrderLines) and ESlines is always on the fly, save them, then go thru.
  - What happens when AABB matches but C_BPartner doesn't? (e.g. ST_SH has same AABB as SH but different C_BPartner). Is that EN-BLOC or EXPLODE?
  ** Explode**
  - Is EN-BLOC just an optimisation (skip the walk because we know the answer), or is it semantically different (the furniture set is treated as one atomic unit)?
  ** Yes because we found out the answer is only one. If more than one, yes explode happens. **
  
  (** in future Bonsai GUI, that singularity has only 1 orderline. The user can edit otherwise click on any part of the en bloc model and modify to explode which way user prefers - add a bath, add a balcony, increase room size - all those results in more orderlines as they are DSL decisions. ESLines are generated for them with default positioning spots which they can also edit. During pure IFC era, everything if flat coord positioned. Here we have relationships. We are introducing design semantics.) **
	
  2. The 61 DX furniture c_orderlines — what replaces them?
	**Not sure what 'replaces'. Such a case is EXPLODE scenario. In Singulartiy there is only 1 top orderline for DX (matching) and only one such version in {PREFIX}_BOM.db thus it is taken en bloc. Seldom in real life but this is only for our first basic p-o-c truth test. 'Can this plane even take off'? This remains our most basic regression test.**
  PROGRESS says "replace 61 individual furniture c_orderlines with room-level BomCategory references." But:

  - Does each room become ONE c_orderline pointing to a BomCategory (e.g. LI_DX), replacing the 61 individual chair/table/sofa lines?
  - Or does the BomCategory lookup happen at compile time without changing c_orderline at all — the compiler just reads the BomCategory template instead of individual lines?
  - This choice affects whether c_orderline is "what the customer ordered" (declarative) vs "what the compiler needs" (procedural).
  **OK i see what u mean. Yes each room has to have a 'shadow' BOMCategory or follow a definition. As said, semantics. It is like if that shape is not defined it does not exist. Thus a floor is a category of 'room' or 'space'. It is like what XML did to HTML. TBL's The Semantic Web. Here i am defining likewise a Semantic IFC/BIM.
  In the end we have tons of components in library.db and they all have their 'passports' in BOMCategory. This is my vision and i throw names onto them to see which stick. Some may not this is where you vast analytical skills helps me here.**

  3. CO_EmptySpace L2 lines: measurement or intent?
  **COESL is measurement aid. Similar to SAP's WMS EmptyStorage. It basically a tag to each Orderline to say 'this is where u put it' so as to not to clutter Orderline 'What'
  Yes COL is intent COES is measurement.**

  L0 = building, L1 = storey, L2 = room. But:

  - Are L2 lines the room's available space (what you can fill), or the room's occupied space (what got placed)? **Again What and Where. Buffer filler space concept i brought in due to a room has to have empty space as opposed to the rest of a building. A roof, wall, floor, MEP are filled up. Habitable space not. Furniture cannot be crammed in (as seen in tests which spurred me to the concept). They are nicely arranged in corners or evenly central.**
  - L2 AABB comes from ad_room_boundary — that's design-time. But if EXPLODE walk writes new items, does the L2 line get updated to reflect actual placement?
  - Who consumes L2 lines? VERIFY PLACEMENT checks counts, but does anything use the L2 AABB for actual placement decisions?

  4. Rosetta Stone: what does "SpatialDigest equality" actually prove?

  The acceptance criterion is SpatialDigest(SH) == SpatialDigest(ST_SH). But:

  - SH is EXTRACTED (56 elements from IFC). ST_SH is GENERATIVE (123 elements from BOM compilation). How can they have the same digest when element counts differ?
  **They cannot differ. Each element must be same one in component_library.db. This is another very basic Singularity test**
  - Is the target that ST_SH should eventually produce exactly 56 elements matching SH? Or is the digest formula going to be relaxed to compare only structural/furniture classes?
  **No relaxation. This is a deterministic DAG Compiler. First Principle**
  - What's the actual proof of equivalence? Same furniture in same positions? Same room boundaries? Same total AABB?
  **This is where the Spatial Digest or i prefer to call it COBOL era Hash-Total of its elements + spatial volumes comes in. Those must match RosettaStone. AABB too.**

  5. TB-LKTN: GENERATIVE with no reference — what's the acceptance criterion?
  **This is where we are out of the RosettaStone confines of an ad verbatim reference. This is where it has to follow rules already setup. Coupled with UBBL and 2D layout grids (a secondary reference in DSL). Early trials were dismal as a primitive stone age. Your LLM has not much guidance. Most invented data. Led me to such modeling heavily borrowing from iDempiere AD, DocAction and BOM model.

  TB-LKTN has no reference IFC, so no Rosetta Stone. Currently:
 **Answered above hopefully.**
  - What proves TB-LKTN is correct? Grid-line alignment? Room dimension match to the PDF drawings? Fixture count from the drawing schedule? **Architect expectations - accurate to 2D which as confided above is near impossible unless a powerful language is there to assist. Thus even BIM COBOL was introduced just yesterday. It may still need 2D verbs**
  - The "INVENTION STOP" rule says compiler must halt if component_library returns nothing. What triggers re-test? User creates LOD mesh, adds to component_library, then re-runs?
  **This is BIM Development process cycle. Team must get all the components ready in library. Every wall, mesh, object. Mesh2Library.txt whenever needed. As i know, a mesh can be altered slightly to fit, ie. for door, window, roof. Or extended with their tiles etc. But the basic LOD must be there or else it will go wild as experienced. Each finished product LOD should be stored with proper semantics in the library so that future users need not recreate again. It is like inventing a new screw design and keep it in case another similar spec comes along.**
  - Is TB-LKTN's expected_elements=139 a hard gate or an approximation that will change as more components are added? **Of course enriched or enrichable.**

  6. VerbStage: parse-only forever, or will it execute? **This was done by you when implementing the BIM COBOL paper. It is as the name and paper suggests, a highler level BIM language. So do advice further**

  VerbStage (Stage 6) currently only parses .bimcobol files, doesn't execute. The blocker is "circular dep: DAGCompiler cannot depend on BIM_COBOL." But:

  - Is the end goal that VerbStage replaces hardcoded MEP placement (placeMEPSprinklers, placeHVAC, placeElectrical)? **If u concur**
  - If so, how do you break the circular dep? Options: (a) move verb execution into DAGCompiler directly, (b) plugin/SPI interface, (c) verbs become a post-compile step outside the
  pipeline **it is meant to directly - but been new similar to component library, it is part of the team work, continously improve it to a mature language. So what is the pattern for evolving language?**
  - Or is BIM COBOL intended to stay external — a separate tool that reads output.db and produces a report, never part of the compilation pipeline?

  7. Terminal recomposition: scope and timeline

  TE-1→TE-8 roadmap covers 51,092 elements across 9 disciplines. But:

  - Is Terminal a compile target (GENERATIVE, like TB-LKTN) or forever EXTRACTED (reference-only)? **It is our third RosettaStone. It is already fully extracted. Its p-o-c after all this runs well is to go next.
  - If it becomes GENERATIVE, does the same pipeline handle a 50K-element commercial building and a 56-element house? **Thus yes**
  - The {PREFIX}_BOM.db currently has zero Terminal BOM entries. What's the first step? Extract BOMs from Terminal IFC, or design them from scratch? *Either way. If the BOM relationship rules are robust then use them otherwise we craft them out. Expect to be tedious but much decisions should be intuitve. i.e. a Hall is a BOM. A seating arrangement, a rest room, stack of floors. That large roof with dome. Walls with awnings. Perhaps even multiple BOMs or different Prefabs for a single construct. Tall wall with awnings+over openings, without openings. with glass walls. Same material, same basic standard shapes for easy install.**

  ---
  These 7 questions map to the gaps between what's documented and what the compiler needs to do next. Which ones do you want to tackle first? **Your vast analysis can reconcile them all and i welcome more questions as i trust this process to arrive at a coherent model first. Otherwise it is GIGO.**


ROUND 3.
Q1. Can I investigate the 67 phantom elements now? Open both output DBs, compare element class distributions, report what ST_SH has that SH doesn't. Data question — answerable with   code.     **Yes**                                                                                                                                                                        
  Q2. output.db c_orderline schema: Same as {PREFIX}_BOM.db's c_orderline, or simpler "generated order" schema? Does {PREFIX}_BOM.db's c_orderline table stay as user input, or migrate entirely to     
  output.db? **They should all be in output.db to avoid contaminating {PREFIX}_BOM.db which supposed to be a model dictionary not a wikipedia or essays derivation.

  Q3. BomCategory codes for non-rooms: Fine-grained (WE/WI/WP for wall types, EL/PL/HV for MEP) or coarse (WL for all walls, ME for all MEP)? **BC codes are like Materials Management UPC/EAN codes. They give qualification to a wall with openings and even down to the door having its finer details outside its component_library basics. They can say a certain fitting category is 'curtains' its priority or replacement or attending prerequisites. This is all towards building a rich MM of ERP.**

  Q4. VerbStage: do verbs generate geometry or just reports? ROUTE SPRINKLERS computes placements — should those become real elements in the build, or stay read-only analysis? They should be the classes that is like Java to native code. As said, COBOL is to assembler that layusers can understand. Architects cannot understand code. But excel or SQL english like. Ask me again in case i am not answering as expected or i misunderstood. **

  Q5. User roles: All one person (you), or multi-person team? Affects tooling complexity. **Oh i see. Well i think a single standalone user for now. But certainly multi user is the future. That can be later right? 

  Q6. Batch or interactive compilation? run_tests.sh vs Bonsai live recompile per edit. Similar to COBOL and ERP, should be batch or process based. Ie. after doing some changes, user saves to let it updates/process and refresh the results.

ROUND 4  
  Q1. Rosetta Stone approach: align BOM (A) or semantic digest (B)?
  - (A) Align BOM to SH representation: The SH BOM must produce the EXACT same 56 elements as the SH IFC — walls stay as IfcWall (not decomposed into plate+column+beam), furniture must have SH's exact dimensions from component_library, no MEP. This means the GENERATIVE path for SH must recreate the architect's original model faithfully. **if decomposing fulfil the model semantics yes but must maintain visual exactness and IFC integrity fordownstream 4D thru 7D.**
  - (B) Semantic equivalence digest: Accept that GENERATIVE decomposition is richer/different. Define equivalence at room/function level: same rooms, same furniture types, same AABB,
  same functional purpose. The digest compares "semantic intent" not "element identity." **Element Identity otherwise we have no reference of non drfit in compilation - the big pain**
  - Which path? Or a third option?

  Q2. SH wall representation: monolithic or structural framing?
  The SH IFC has IfcWall (monolithic). ST_SH decomposes into structural components (Column + Beam + Plate cladding). Which is the correct model for the compiler to produce? If
  monolithic walls for SH but structural framing for larger buildings — how does the BOM model express this difference? **I am just learning from you this with thanks. I may understand this instill the STR discipline which is good as it enrich our BIM value. Thus this also means we cannot hash-total the element count but the vertices of BBoxes? At least to mathetematically tell us that things are visually the same without need to examine manually which is tedious. By such check, u may reiterate right away what broke the placement.**

  Q3. Furniture dimension mismatch: library must match IFC?
  SH has bed_2032x1980x500mm, ST_SH generates bed_1980x2032x634mm (swapped dims, different height). Should the component_library contain the exact IFC furniture items (so the compiler picks the right one)? Or are approximate matches acceptable for Rosetta Stone? **Yes exact, or else drift and uncertainty occurs. It must be deterministic. Only in TBKLTN, new cases there is allow drift during compilation but errors will be narrower. If at Rosetta Stone stage drift occurs, it is disastrous downstream. WDYT?**

  Q4. MEP in Rosetta Stone: include or exclude?
  SH IFC has zero MEP elements. ST_SH generates 43 MEP elements. For the Rosetta Stone comparison, should MEP be: (a) excluded from digest entirely, (b) added to SH's reference IFC
  retroactively, or (c) something else? **Of course excluded. The digest or hash total test is only for visual confirmation. As said it takes me countless cycles of launching Bonsai and loading to know something is off. I don't want that. I want the maths to nail it.**

  Q5. The 199mm door data — fix or investigate?
  Two ST_SH doors are 199mm wide (barely a slot). Is this a BOM data error that should be fixed in migration, or is it intentional (e.g. narrow service openings)? **SH is a good IFC sample i downloaded and examined to be pristine. I am not aware of such mishapes. Trace back the extracted Db or IFC which should be exact.**

  Q6. C_Order header: stays in {PREFIX}_BOM.db or moves to output.db too?
  You said all c_orderlines should go to output.db. Does the C_Order header (the building project record) also move, or does it stay in {PREFIX}_BOM.db as project metadata? **moves to output DB.** 
  
  ROUND 5
    Q1. Two-layer digest: how should the BBox vertex hash work?
  For structural zones, the BBox of the composed assembly (all columns+beams+plates for one wall) must match the monolithic IfcWall BBox. But how do we GROUP the decomposed elements
  into "one wall"? Options:
  - (a) By spatial proximity — elements within ε of each other form a group. 
  - (b) By BOM lineage — m_bom_line parent groups all children
  - (c) By storey+wall_face — all structural elements on the same wall face form one group
**The issues mostly occur at walls/openings/furniture orientation,rotations,placements. So i reckoned something to check against all such drifts. Welcome your ideas. But since they must extract the components and place in the output.db which must corelate to the extracted db, then take a certain x-cross sampling that must be exact. i.e. a piano orientation, a furniture spatial distance from another. Another type of error is been none LOD, but BBox, no material and invented drawing. Thus that LOD to LOD check should cover all that. We been going thru series of tests. But in essense it is just those.**

  Q2. Furniture product_id matching: how does the compiler select?
  Currently ST_SH picks bed_1980x2032x634mm where SH has bed_2032x1980x500mm. The exact SH items exist in component_library (e.g. IfcDoor_Ifc4_SampleHouse_c5357415). Should the ST_SH
  BOM explicitly reference these SH-extracted product_ids? Or should the compiler match by type+AABB and we need to fix the matching algorithm? **It should pick by exact AABB as we are still singularity stage where there cannot be another similar size bed. If there is, it is still OK, but i also shared in previous chat session that if the parent so happens to fit AABB then take en bloc all their furniture sets. This is to avoid buffer filler replacings which can misplace and break the tests. This is not an hard code but feature allowing non parent fit to recalculate furniture sets placing. For example in SH living room, the AABB is exact, thus take all the contents in priority (dining, sofa, piano) sets with their buffer fillers. In TBKLTN the living room AABB doesn't fit, thus spawn 3 orderlines with attending EmptySpaceLines for each set in priority: Dining first put as ESLine given a good central or best spot first priority. Then it looks for any further room if any fit in the sofa. If it can't fit try fitting the others - piano, the ESline provide a calculated remaining spot which won't clash the piano bbox with others. This is first time i am going into the specifics which only happens during TBKLTN. So this gives u more context. For our Stones cases, as explained a parent (living room) AABB fit allows wholesale port, thus only one ESLine at that room orderline.

The TBKLTN will have fillers added in between if that TBKLTN living room is to be committed as a new BOM to {PREFIX}_BOM.db (can be an option later)**

  Q3. The 2 extra ST_SH doors — delete from BOM or keep?
  ST_SH has 5 doors, SH has 3. The extra D7 and D2 come from the BOM model. Should the ST_SH BOM be fixed to produce exactly 3 doors matching SH? Or are the extras intentional
  structural additions (e.g. access doors that the architect didn't model)? **remove such unknown inventions for now**

  Q4. output.db c_orderline schema: mirror {PREFIX}_BOM.db or redesign? **hm, not sure. In ERP speak, it should retain transactional instance information to allow tracking later. Date, Description.**
  Since C_Order+C_OrderLine both move to output.db, should the schema be: (a) exact copy of {PREFIX}_BOM.db's current tables, (b) a simpler "compiled order" schema with only the fields the
  pipeline needs, or (c) {PREFIX}_BOM.db schema + extra compile-time columns (source=USER|EXPLODE, compilation_id, etc.)?

  Q5. IfcFlowTerminal "tall_cabinet" — reclassify or delete?
  Two IfcFlowTerminal elements in ST_SH are actually tall kitchen cabinets (furniture). Fix in: (a) component_library — change their ifc_class, (b) the BOM model — change the M_Product
   entry, or (c) the compiler — add a classification override? **Wrongful classified? Investigate whose fault. It should follow other furniture IFC. IFC naming and grouping by BOM tree is also important to allow the GUI later to handle easily**

ROUND 6
Q1. Wholesale port trigger: exact AABB or within tolerance?
  "Exact fit" for parent AABB — does this mean room_AABB == BomCategory_AABB to the millimetre? Or is there an ε tolerance (e.g. ±50mm)? A real room might be 4695mm vs a template of
  4690mm — should that count as "exact"? **exact** so our singleton and explosion works for exact Rosetta Stone replication. Also we like it to be as deterministic as i regard this an engineering field where precision cannot be 2nd guessed. And this can be our hallmark - high precision. Is this what is expected in the industry?**

  Q2. Priority ordering: where is it defined?
  Dining→sofa→piano — is this priority encoded in the BOM model (e.g. M_BomCategoryLine.Sequence), or is it a global rule, or per-room-type? Where should priority numbers live in the
  schema? **Yes, user defined, in this case, it is my opinion. They act as defaults until user changes them again.**

  Q3. BOM commit workflow: who triggers it?
  When a TB-LKTN living room arrangement is satisfactory and should be committed as a new BOM — is this an explicit user action in Bonsai GUI ("Save as BOM"), or does the compiler
  auto-detect stable configurations? And does the new BOM go to {PREFIX}_BOM.db (model dictionary) or a separate user-BOMs database? **For now default to yes and same {PREFIX}_BOM.db.**

  Q4. TOILET_BLOCK_FIXTURES → IfcFlowTerminal: also misclassified?
  The m_bom_line has TOILET_BLOCK_FIXTURES → child_product_id='IfcFlowTerminal'. Toilet fixtures should be IfcSanitaryTerminal (basin, toilet, bidet). Is this the same bug pattern as
  the kitchen cabinets, or are flow terminals intentional for toilet plumbing? **Classify them correctly then. Use your discretion.**

  Q5. Furniture matching for Rosetta Stone: by product_id name or by AABB? **By AABB and same inferred category = Residential and their BOM 'DX' nature. (please stitch this somewhere were BOMCategory DocType is 'Residential' etc. Otherwise when one day we have an AABB for let's say vehicle it surely be looking for different categories.**
  You said "pick by exact AABB." Currently the SH furniture items exist in component_library with names like IfcDoor_Ifc4_SampleHouse_c5357415. Should the compiler match by:
  - (a) Product name/ID containing the building source (e.g. _Ifc4_SampleHouse_)
  - (b) Pure AABB match against component_library
  - (c) m_bom_line explicitly references the SH-extracted product_id
  **Unless otherwise, i would say space size and category will pick out the right intended item. Again as we only have singularity sample, it be sufficient to be the same.**
**Help in cleaning that part up, putting in the right values as required by the models**

ROUND 7
1. C_Order bootstrap paradox                                                                                                                                                          
                                                                                                                                                                                
  You said "C_Order moves to output.db." But c_order is what tells the compiler WHAT to compile — building_id, provenance (EXTRACTED/GENERATIVE), c_bpartner, output_db_path. The  compiler reads it at startup before the output DB exists.                                                                                     
  Split candidate: project config fields (building_id, provenance, c_bpartner, output_db_path) stay in {PREFIX}_BOM.db; compile-time results (spatial_digest, expected_elements,    empty_space_checksum) move to output.db. Or: the entire c_order moves and the compiler receives building_id as a CLI parameter, creating the output DB from scratch each run. Which?  
**Should initiate a fresh output db and then populate till end. Don't see this as ambigous. None of it disturb the {PREFIX}_BOM.db.**

  2. Structural doors are NOT from BOM
  I traced the 5 ST_SH doors — they come from the structural compilation path (WALL_PANEL / c_orderline structural entries), not from the BOM tree. The SH BOMs (UNIT_SH_STD →
  FLOOR_SH_GF_STD) only have furniture + toilet + slab + roof. Walls, doors, windows, columns, beams are all structural pipeline output.

  So "remove 2 extra doors from BOM" is the wrong fix — the structural emitter is generating them. Should I: (a) fix the c_orderline structural data that drives door generation, (b)
  fix the structural compilation code, or (c) is this a signal that doors/windows should move INTO the BOM tree?
  
  **Aren't everything LOD material in the extracted or orginal IFC? Otherwise first principle of extract not inventing was broken and cheating crept in. Hope this is clear and you resolve all accordingly. All such drift blocks should be inspected why they allow thru. Lock back.**

  3. Furniture AABB: 3D exact or 2D footprint? **This is obviously 3D, Do not drift from clear explanation and become rhetorical.**

  SH bed = 2032×1980×500mm. ST_SH bed = 1980×2032×634mm.
  - Width/depth are swapped (different rotation/orientation)
  - Height differs: 500 vs 634mm
**Then investigate why they differ. Must be same bounding box. Where are they located? The flow is clear - from IFC to component_library.db. {PREFIX}_BOM.db merely organisation holding the AABB bbox data exactly.**

  "Exact AABB" — does this mean exact 3D match (fails here), orientation-independent sorted dims (still fails due to height), or 2D footprint match ignoring height (passes if we sort:
  1980×2032 = 1980×2032)?

  4. Digest grouping on the EXTRACTED side
  Two-layer digest groups structural elements by BOM lineage on the GENERATIVE side. But the EXTRACTED side (SH) has flat IFC elements with no BOM tree. Its 5 IfcWall elements have no parent grouping. How do I form comparison groups on the SH side to match against ST_SH's column+beam+plate assemblies? By ifc_class? By spatial envelope overlap? Or does the
 extracted side just hash its walls as-is and the generative side must produce the same hash from its decomposed parts?
**Propose back as i already made clear what i want. Something to avoid visual checks. Based on maths. That says 100% it is GIGO. output = input. Without cheating**

  5. Wholesale port: does individual furniture position exist anywhere?
  Wholesale port = one C_OrderLine + one ESLine for the entire room. But the Rosetta Stone proof needs to verify the piano is at the right position, the sofa is facing the right way.
  Does the compiler:
  - (a) Still compute individual positions internally and write them to elements_meta (just not as separate ESLines) **they undergo same transformation. It is like taking a prefab living room and transporting it to the right spot facing correctly relative to the parent container layout.**
  - (b) Trust the BOM layout entirely — no per-item position verification for wholesale rooms. **En bloc means trust in total. If it is not correct, means its the formula and library info. GIGO somewhere.**
  - (c) The digest handles this by checking element BBox positions directly in elements_meta/RTREE, bypassing ESLines **ESLines are just place position holders. By right digest should check output.db actual element positioning. Apple for apple isnt it?**

  6. DocType vs C_BPartner_ID vs _SH/_DX suffix overlap

  M_BomCategory already has C_BPartner_ID column (currently only 'ST' for RE). BomCategory codes have _SH/_DX suffixes (LI_SH, LI_DX). Now we're adding doc_type
  (Residential/Commercial). Three scoping mechanisms for the same concept. Does doc_type replace C_BPartner_ID? Replace the suffixes? Coexist as a separate hierarchy level?
  **This is where it finds the fit by AABB and category. If cateogry is same as in the ST RE case, it really finds the exact singular fit. If it is the same, it is a good problem. Then we know there are 2 exact fits of same category.**
  
  7. expected_elements: hard gate or auto-calculated?

  ST_SH currently expects 123. After data fixes (door removal, reclassification), the actual count changes. Is expected_elements:
  - (a) A manually-set gate that I must update in each migration
  - (b) Auto-calculated by the compiler after each run (compiler writes the count, next run uses it)
  - (c) Only meaningful for EXTRACTED buildings (truth from IFC), irrelevant for GENERATIVE (compiler determines the count)
 **Are you overthinking here? All my explanations since few rounds ago should be clear what i am after - maths that proves visuals without cheating exactness between input and output**

  8. VerbStage pipeline position for element generation
  Current pipeline: Compile → Write → Verb → Prove → Digest. If verbs generate real elements, they run AFTER Write has already written to the output DB. The new elements need writing
  too. Options:
  - (a) Verb moves before Write (verbs produce PlacedElements, Write outputs everything)
  - (b) Verb writes its own elements directly to output DB (second write pass)
  - (c) Verb stays after Prove — a post-compilation enrichment step, not part of the core pipeline

  This affects whether verb-generated elements participate in ProveStage quality gates and DigestStage hash computation.
 **Follow your best practice known to you when dealing with 'COBOL' as an evolving language taken over assembler in parts**
  ---
  Everything else (TOILET_BLOCK reclassification, element naming fix, SPI interface shape, doc_type value list, migration scripts) I can infer from existing patterns. These 8 are the
  ones where my guess could diverge from your vision.


