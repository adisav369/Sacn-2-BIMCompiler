# OPEN ISSUES — single source of truth
# Coder: cannot close session while High issues exist without Watchdog sign-off
# Watchdog: removes a line only when a § log line in the session proves it fixed
# Format: [severity] I-## description (spec ref)
# Language: English only

[High]   I-21  All MEP symbols (fan, switch, socket, light etc.) not sourced from IFC extraction, not stored in input DB, not placed from template. fan.png exists unused. Must extract from IFC, store in 2D.db, render from template — request user for any symbols not in IFC. (§17, §14.3)
[High]   I-22  SH floor plan and right panel (title block, schedule) are correct and consistent. DX and other buildings do not follow the same standard — suspect extraction rules are building-specific not abstract. All buildings must use the same generic extraction path. (§3 extraction rules)
[Low]    I-07  JKR logo not in title block header. (§14.2)
[Low]    I-10  PINDAAN NO revision row absent from title block. (§14.1)
