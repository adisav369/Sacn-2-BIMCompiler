# OPEN ISSUES — single source of truth
# Coder: cannot close session while High issues exist without Watchdog sign-off
# Watchdog: removes a line only when a § log line in the session proves it fixed
# Format: [severity] I-## description (spec ref)
# Language: English only

[High]   I-21  MEP terminal symbols must use element footprint from elements_rtree (§17.7), not symbol_code lookup. Current code draws circles/crosses per keyword match — replace with LWPOLYLINE of element bounding box. Test: §SYMBOL_FOOTPRINT log per terminal, zero circles on MEP layer. (§17.7)
[High]   I-27  DX multi-storey floor plan title is hardcoded "FLOOR PLAN" for both GF and FF. Must be storey-qualified: "GROUND FLOOR PLAN" / "FIRST FLOOR PLAN" — derive from `2d_drawing_type.drawing_title` for each storey code (GF→"GROUND FLOOR PLAN", FF→"FIRST FLOOR PLAN"). Drawing number also needs storey suffix (A-01-GF, A-01-FF). Spec §12.1 test_storey_labels: "sheet title contains storey name, sheet number contains storey code". (§12.1)
[High]   I-28  MEP plan drawing title is hardcoded `f'{discipline} LAYOUT'` → "PLUMBING LAYOUT". Title must come from `2d_drawing_type.drawing_title` (add rows: M-01="GROUND FL PLUMBING", E-01="GROUND FL ELECTRICAL"). Caller reads title from page table (`DX_2D.json`), passes to `write_mep_plan_dxf(drawing_title=...)`. Log label `§D MEP plan discipline=PLUMBING` must also use friendly display name from `2d_drawing_type.description`. (§12.2)
[High]   I-29  Grid bubbles are INSIDE the dimension tiers on all views (plans and elevations). Confirmed from SVG: bubbles at cy=29.748, dim lines at y=19–25 (dims FURTHER from building than bubbles). Spec §6.1 and §7.1a require: tiers stack between building edge and grid extension; bubble is at the FAR END of the grid line (furthest from building). Fix: place tier dims close to building edge, then extend grid line + bubble BEYOND outermost tier. Template params: `dimensions.tier_1_offset_mm`, `dimensions.tier_2_offset_mm`, `grid.extend_beyond_building_mm`. Log: §GRID_BUBBLE_Y, §DIM_TIER_Y proving bubble_y > tier_1_y > tier_2_y > building_edge. (§6.1, §7.1a)
[High]   I-30  `2d_drawing_symbol` and `2d_drawing_style` rows for MEP IFC classes missing from 2D.db. Without these, I-21 cannot be completed (no footprint source) and all MEP elements render as fallback circles. Spec §12.2 gives explicit SQL for IfcPipeSegment, IfcDuctSegment, IfcFlowTerminal, IfcSwitchingDevice, IfcElectricAppliance. Add as DB migration. Test: `test_mep_symbol_coverage` — every MEP IFC class has a `2d_drawing_style` row. (§12.2)
[Low]    I-07  JKR logo not in title block header. (§14.2)
