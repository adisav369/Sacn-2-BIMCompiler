# ⚠ DO NOT REMOVE — Scope guard / FABLE-5 CARD: B-3 — 0-seed POSTING oracles (the NAMED SEQUEL to B-2)
# Scope: the doc classes whose FSM is source-parsed but that have ZERO posted `fact_acct` rows in ANY seed DB,
#   so their accounting fan-out has NEVER been oracle-verified. Targets (ad_docfsm.js `DOC_FAMILY`, the two
#   "0-seed" comment bands at build/erp/ad_docfsm.js:185 and :198):
#     · C_BankTransfer (200246) · C_DepositBatch (200056) · M_ProjectIssue (623)
#     · Fixed-Asset family — A_Asset_Addition (53137) · A_Asset_Disposed (53127) · A_Asset_Reval (53275)
#       · A_Asset_Transfer (53128) · A_Depreciation_Entry (53121)
#   Each has FSM logic parsed from source (H-2 ISOMORPH tail) but NO real posted document anywhere → their
#   `Fact_Acct` fan-out is the LAST un-oracled accounting surface. This card oracle-verifies it.
# MODEL LANE: Fable 5 (deep reasoning + 1M context: the compiled org.compiere.acct Doc_* classes + the
#   MAcctSchema/GL-category/period setup they need + our doc_poster verb, all held at once). This is NOT
#   mechanical replication of an existing trace (B-2/H-1 had real rows to diff) — the whole crux is there is
#   NOTHING to diff yet: the oracle must be GENERATED by driving the REAL compiled posters headless on a
#   scratch PG, then Fact_Acct diffed row-by-row. Irreducible get-it-right-first-time correctness loop =
#   exactly the Fable lane (memory `project_fable5_lane`, `feedback_model_allocation_mastermind_vs_execution`).
# Headless, bim-compiler ONLY — NO bim-ootb, NO deploy, NO sw. READ THE LOG after every run (exit ≠ evidence).
#   ALL poc_* via `bash build/erp/run_witness.sh scripts/poc_X.js`.
# NON-NEGOTIABLE: EXTRACT/GENERATE-THEN-DIFF, NEVER INVENT — the oracle is the REAL compiled Doc_* poster's
#   own output on a real (scratch) PG, captured like extract_fact_acct.sh does for GardenWorld. A class that
#   genuinely cannot post headless without forking semantics is an HONEST ⛔/SKIP (named + counted, the
#   WorkflowOracle un-headless precedent), NEVER a synthesized Fact_Acct. A class that posts ZERO by CONFIG is
#   the poc_morder_post case (zero is the correct oracle) — prove zero-is-config-derived with a gate-flip
#   §FALSIFIER, do not skip it. Spec-first; §FALSIFIER load-bearing; integer-cents metric.
#   docs/ERP_BACKEND_SEPARATION.md seams stay intact.
# READ FIRST (in order):
#   1. prompts/FABLE5_WORKFLOW_ORACLE.md — B-2, the DIRECT precedent this card sequels (§W-1..§W-5 skeleton,
#      generate/replay-then-diff discipline, small-K honesty). Its STOP CONDITION names THIS card by name.
#   2. scripts/poc_post_harden.js (W-POST-HARDEN, HARDEN_MATRIX §H-1) — THE POSTING TECHNIQUE THIS GENERALIZES:
#      post_resolver's derived GL lines == real fact_acct per (natural-account, side) in INTEGER CENTS,
#      maxDiff=0c. Oracle = GardenWorld client-11 fact_acct captured by scripts/extract_fact_acct.sh into
#      build/erp/glassbowl_data.db, NEVER hand-authored. Same move, pointed at classes with no seed rows.
#   3. scripts/poc_factacct_doc.js (W-FACTACCT-DOC, §H-1.1) — per-document granularity (ad_table_id/record_id/
#      line_id) row-by-row vs live PG. The capture shape the new classes must extend.
#   4. scripts/poc_morder_post.js (W-MORDER-POST, §H-1.2) — the CONFIG-GATED ZERO case: Doc_Order posts 0
#      fact_acct rows because GardenWorld CommitmentType='N'; §FALSIFIER-A flips the gate to prove zero is
#      config-derived, not hardcoded. Several B-3 targets will land here — copy this proof verbatim.
#   5. scripts/logic_oracle/{LogicOracle,WorkflowOracle,ConfirmOracle}.java — the "drive REAL compiled classes
#      headless, name every omitted static in the header" pattern. B-3's PostingOracle.java mirrors these,
#      pointed at org.compiere.acct.Doc / Doc_BankTransfer / Doc_DepositBatch / Doc_ProjectIssue / Doc_Asset*.
#   6. build/erp/ad_docfsm.js:185-205 — the exact 0-seed target rows + their per-class M*.java line citations
#      (already parsed); prompts/H2_ISOMORPH_TAIL.md — how the 0-seed FSM tail was honestly handled (⛔ on
#      stored-replay, source-parse FSM only) — B-3 is the POSTING counterpart of that same honesty.

---

## ⚠ TRIAGE RESULT + USER RULING (2026-07-17 — reshapes, but does NOT kill, §W-2)
Triage (W-POST-B3, `build/erp/poc_post_b3.log`, exit 0) found **all 8 targets have ZERO source documents**
in GardenWorld (the only real seed), not just zero posted rows. Two arms:
- **2 are ∅-BY-DESIGN — DONE** — C_BankTransfer / C_DepositBatch have **no `Doc_<Class>` in the posting
  factory** (`org.compiere.acct.Doc.get` has no case) → posting is not in their contract. oracle=∅ is CORRECT
  and structurally proven. Closed.
- **6 are G-seed — ASSIGNED TO FABLE 5** — C_ProjectIssue + the 5 Fixed-Asset classes: poster EXISTS,
  manifest source-parsed + line-cited (§W-1 below), but 0 source docs → nothing to post yet.
- **USER RULING (2026-07-17):** preparing seed documents **along the GardenWorld model** is **usability / PoC
  seed prep — NOT a prime-rule violation.** The oracle stays the REAL compiled poster's output over that seed;
  only the demo INPUT is prepared, honestly labelled as seed. So §W-2 IS legal and IS the path — it was never a
  synthesis of the oracle. (What the prime rule still forbids is HAND-AUTHORING the expected `fact_acct` — that
  stays banned. My earlier "⛔ n/a-in-seed / prime-rule" reading over-applied the ISOMORPH-tail rule; corrected.)
- **Lane:** the G-seed generate arm is a **Fable 5 session** (deep get-it-right-first-time posting loop). This
  Opus session did the triage + manifest source-parse + hand-off; the Fable session executes §W-2→§W-5 for the 6.

## FACTS (verified live 2026-07-17 — Docker `postgres` / db `idempiere_test`, client 11 GardenWorld)
- **All 8 targets: `fact_acct` rows = 0 AND source-doc rows = 0** (c_banktransfer / c_depositbatch /
  c_projectissue / a_asset_addition / a_asset_disposed / a_asset_reval / a_asset_transfer /
  a_depreciation_entry all `count(*)=0`; captured oracle `glassbowl_data.db` fact_acct=300 rows, none for
  these ids). Re-asserted in W-POST-B3, not assumed.
- **Posters present** (org.compiere.acct): Doc_ProjectIssue, Doc_AssetAddition, Doc_AssetDisposed,
  Doc_AssetReval, Doc_AssetTransfer, Doc_DepreciationEntry. **Absent**: Doc_BankTransfer, Doc_DepositBatch
  (grep of Doc.java factory + acct package — structurally non-posting).
- These 8 classes have FSM parsed from source (ad_docfsm DOC_FAMILY, H-2 ISOMORPH tail) but **0 posted
  `fact_acct` rows** in the GardenWorld seed — confirm per class: `SELECT count(*) FROM fact_acct WHERE
  ad_table_id = <id>` = 0 for each. That zero is WHY posting was never oracled — state each K honestly.
- The posting oracle for the ALREADY-covered classes lives in `build/erp/glassbowl_data.db` (fact_acct, 300
  rows, ΣDR=ΣCR=46574.97 — the test_report_fin anchor), captured by `scripts/extract_fact_acct.sh` from the
  same live PG. B-3 EXTENDS this capture; it must NOT perturb the existing bundle (poc_factacct_doc +
  test_report_fin must still pass after any extract change — the H-2 additive-block rule).
- The engine verb under test is `build/erp/doc_poster.derivePostings` (W-DOC-POSTER) — the extraction that
  folds a document to its GL manifest. B-3 asks: for each 0-seed class, does derivePostings reproduce what the
  REAL compiled Doc_<Class> poster emits — including the correct ZERO where config gates posting off?
- The oracle for the 6 G-seed classes is GENERATED (per the USER RULING above): prepare GardenWorld-model seed
  documents, drive the REAL compiled `Doc_<Class>` on a SCRATCH copy of `idempiere_test` (never the shared
  instance) with the acct setup it needs (MAcctSchema, GL category, open period, the class's default accounts),
  let iDempiere post, capture that fact_acct exactly as extract_fact_acct.sh does. Provenance = the real
  compiled poster over honestly-labelled seed. The expected facts are NEVER hand-authored — only the seed input.
- Fixed-Asset family: `a_*` doc tables are empty AND the depreciation batch is the DepreciationPerf lane
  (memory `project_depreciation_perf`) — check for overlap before generating; a class already oracle-folded
  elsewhere is CITED, not redone (the ISOMORPH_TAIL rule).

## THE WORK
### §W-1 Triage each target (which arm — zero-by-config, generate, or honest-⛔ — BEFORE any code)
For each of the 8 classes read its `Doc_<Class>.createFacts` in iDempiere source (org.compiere.acct) and its
ad_docfsm.js:185-205 M*.java citations, and classify:
- **(Z) zero-by-config** — createFacts is gated off under GardenWorld's captured config (the Doc_Order case).
  Oracle = the empty set; proof = §W-3 gate-flip. No scratch posting needed.
- **(G) generatable** — createFacts posts under an achievable minimal setup. Goes to §W-2 scratch-generate.
- **(⛔) not headless-postable** — needs semantics we cannot stand up without forking (name the exact blocker).
  Honest skip, counted, WITH the reason — the WorkflowOracle un-headless precedent. Never fake its Fact_Acct.
`§B3-TRIAGE class=<name> id=<id> arm=Z|G|⛔ createFacts=<Doc_Class.java:lines> reason=<one line>` per class.

### §W-2 Generate the oracle for arm-(G) classes (real compiled poster on a SCRATCH PG — never invent)
Extend `scripts/extract_fact_acct.sh` (additive block, H-2 rule) or add `scripts/generate_post_oracle.sh`:
clone `idempiere_test` → scratch db, insert the minimal completed document via the REAL model classes (or the
captured GardenWorld setup rows), invoke the compiled poster headless, `SELECT fact_acct WHERE ad_table_id=<id>`
with per-document granularity (ad_table_id/record_id/line_id/account_id/c_acctschema_id/DR/CR in integer cents)
into a versioned fixture (`build/erp/oracle/post_b3_<class>.json` or a glassbowl_data.db additive table).
`§B3-GEN class=<name> scratch=<db> posted_rows=<n> ΣDR=ΣCR=<cents> provenance=compiled-Doc_<Class>`.

### §W-3 Diff derivePostings vs the oracle (the core, integer cents)
For each arm-(G) class: `doc_poster.derivePostings(<doc>)` aggregated by (natural-account, side) == the §W-2
oracle, **maxDiff=0c** (the W-POST-HARDEN metric). For each arm-(Z) class: derivePostings == ∅, and the
§FALSIFIER flips the config gate → BOTH the engine and the real poster emit rows (zero is config-derived, not
hardcoded — the poc_morder_post §FALSIFIER-A verbatim). Any divergence = a NAMED finding (engine gap or a
posting-semantics gap), reported not papered.
`§B3-POST class=<name> arm=<Z|G> maxDiff=0c oracle=compiled-Doc_<Class>` per class.

### §W-4 The semantics arm (Fable-5 judgment — only as far as honesty requires)
Where the diff needs the REAL poster's account resolution (Doc.getAccount, MAcctSchemaDefault, product/asset
accounting), add `scripts/logic_oracle/PostingOracle.java` mirroring LogicOracle/WorkflowOracle: drive the
compiled `Doc_<Class>` account lookup + createFacts headless over the scratch context. Name EVERY omitted
static in the header (CLogger-class drags etc.), exactly as LogicOracle does. A class that cannot run headless
without forking semantics → its (G) claim demotes to ⛔ with the reason; the classes that DO diff still stand.

### §W-5 Witness + falsifiers + named skips
`scripts/poc_post_b3.js` → W-POST-B3 via run_witness.sh → `build/erp/poc_post_b3.log`, exit 0 AND read:
- `§B3-TRIAGE` (8 lines, one per class) · `§B3-GEN` (per arm-G) · `§B3-POST … maxDiff=0c` (per non-⛔ class).
- `§B3-FALSIFIER` ≥2 load-bearing: (1) an arm-Z gate-flip → engine==oracle both emit rows (zero is derived);
  (2) drop/scale ONE derived GL line on an arm-G class → maxDiff≠0c (the diff is load-bearing, poc_doc_poster
  §FALSIFIER shape).
- `§B3-SKIPS` — every arm-⛔ class + WHY, and every FA class deferred to DepreciationPerf (cited, not redone),
  named + counted. The claim is "N classes oracled, maxDiff=0c", NOT "all 8" unless all 8 truly land.
- Regressions after ANY engine/extract edit: poc_post_harden, poc_factacct_doc, poc_doc_poster,
  poc_morder_post, test_report_fin — ALL stay green (the extract bundle is unchanged where it matters).

### §W-6 Bank (same session — this card is its own single writer)
- `docs/ERP_COVERAGE_MATRIX.md`: Oracle column — the 0-seed posting rows ⬜→✅ (or ⛔ with reason) per class,
  full evidence format citing the log; update the ledger headline (43 → 43+N oracle-equivalent).
- `prompts/HARDEN_MATRIX.md` §H-1 posting band → B-3 status. `prompts/FABLE5_WORKFLOW_ORACLE.md` STOP-CONDITION
  B-3 pointer → "opened + banked here". PROGRESS.md §Current State. This card's `# DONE` appendix — every claim
  = a § line (Watchdog protocol).
- Commit bim-compiler (witness + PostingOracle.java + generate script + oracle fixtures + bank docs; prompts/
  is gitignored except FRONTEND_LANE_MASTER — card edits live on disk, note it in the commit message).
- **⏸ PUSH PAUSE is in effect (CLAUDE.md, standing until lifted): commit locally, verify on localhost/headless,
    do NOT `git push` and do NOT open a PR.** Report status; the user lifts the pause.

## STOP CONDITION / HONESTY RAILS
- DONE = every one of the 8 targets is `✅ maxDiff=0c` (posted or config-zero, oracle = real compiled poster)
  OR `⛔ <named blocker>`; W-POST-B3 green with ≥2 load-bearing falsifiers + named skips; regressions green.
  A class whose real poster DIVERGES from derivePostings → the finding IS the deliverable (a real engine/
  semantics gap beats a fake green). A step needing an un-EXTRACTABLE user fact → `⛔ BLOCKED: <the one
  question>`, move to the next class (never loop, never silently drop — WORK-TO-ZERO).
- Do NOT invent a posting where none can be generated headless — an honest ⛔ with the blocker named is the
  correct output, and it is a COMPLETE result for that class, not a failure of the card.
- Do NOT perturb the existing GardenWorld oracle bundle — additive capture only; test_report_fin must still
  balance 46574.97 and poc_factacct_doc must still pass, proving B-3 added rows without forking the journal.

## §W-2 EXECUTION SPEC (2026-07-17, Fable 5 session — written BEFORE code, per Spec-First)
**Host для the real compiled poster:** `org.idempiere.test` tycho-surefire (JUnit-under-OSGi). This resolves the
G-3 landmine (POS_GAP_CLOSE.md: bare `Adempiere.startup(false)` NPEs headless — SecureEngine needs a live OSGi
BundleContext): tycho-surefire hosts the REAL compiled classes inside a real OSGi runtime. The vendor's own
`org.idempiere.test/src/.../model/FixedAssetsTest.java` (IDEMPIERE-5474) is the direct precedent AND the seed
recipe for the addition arm: `MWorkflow.runDocumentActionWorkflow(doc, CO)` completes AND posts synchronously
because `ad_sysconfig CLIENT_ACCOUNTING='I'` (verified live in idempiere_test, client 0).
**Scratch:** `idempiere_b3` := `createdb -T idempiere_test` (never the shared instance — the glassbowl oracle was
captured from idempiere_test, so the clone shares its config byte-for-byte; the DB-snapshot-divergence landmine
says do NOT clone `idempiere` instead). Connection override: `-Didempiere.home=<scratchpad>/b3_home` whose
`idempiere.properties` = the checkout's file with `DBname=idempiere_b3`. Drop the clone after capture.
**Seed plan (GardenWorld-model, honestly labelled; USER RULING 2026-07-17 sanctions INPUT seed prep only):**
1. Vendor recipe verbatim: `MAssetGroupAcct` EQUIPMENT (group 50007, both schemas 101+200000) UseLifeMonths=18
   (GardenWorld ships uselife 0 — the vendor's own test sets this first, div-by-zero otherwise).
2. Asset A (`MAsset` group EQUIPMENT, product ASSET_VEHICLE 200000) + `MAssetAddition` Manual/Capital
   12000/salvage 2000 → CO → posts (Doc_AssetAddition: DR a_asset_acct.A_Asset_Acct / CR getP_Asset_Acct —
   Manual w/o charge falls to ProductCost(P_Expense) — at AssetSourceAmt).
3. `MDepreciationEntry` schema 101, DateAcct = asset A's first depexp month (afterSave auto-selects that month's
   unassigned `A_Depreciation_Exp` rows for the schema+org) → CO → posts per-line DR depexp.DR_Account_ID /
   CR CR_Account_ID at Expense. Client-11 has NO FDP doctype → seed-prep one along the GardenWorld model (the
   FAA doctype 200001 shape, DocBaseType='FDP') — required by prepareIt's testPeriodOpen(getC_DocType_ID()).
   The schema-200000 side is left unentered on purpose — exercises Doc_DepreciationEntry's other-schema ∅ branch.
4. `MAssetReval` asset A, same month as the processed depreciation (prepareIt: isLastDepreciated + BOTH cost and
   accum must change): Cost_Change=cost+1000, Change_Acumulated_Depr=accum+100. GardenWorld has NO reval account
   anywhere → seed-prep: set a_asset_acct.A_Reval_Cost_Offset_Acct (both schemas) := the group's
   A_Disposal_Revenue_Acct combination (200005) — config INPUT, honestly labelled, read identically by poster
   and engine.
5. Asset B + addition → CO; `MAssetTransfer` B (NOT depreciated, not month-end — prepareIt gates): old accts =
   B's a_asset_acct values (group 50007: 200013/200004), new = group 50000's (200010/200011) → CO → posts
   workfile cost old→new (+ accum pair, 0 here). B's workfile is untouched afterwards so the captured workfile
   == posting-time values (order-dependence NAMED).
6. Asset C + addition → CO; `MAssetDisposed` C method=Simple → CO → createDisposal writes `A_Asset_Change`
   (CHANGETYPE_Disposal) per workfile/schema — Doc_AssetDisposed reads THAT row (CR cost / DR accumdep /
   DR disposal-loss=book value). Derive from a_asset_change, NOT the post-adjusted workfile.
7. `MProjectIssue` project 101 (category N → PJ_WIP side), setMandatory(locator 101, product 137 Mulch, qty 1)
   → CO → Doc_ProjectIssue: DR c_project_acct.PJ_WIP_Acct / CR Product Asset (Expense if service) at
   m_line.getProductCosts (MCost current cost × qty).
8. `commit()` (AbstractTestCase default is rollback — we commit INTO THE SCRATCH CLONE only), then
   `scripts/generate_post_oracle.sh` captures fact_acct для the 6 ad_table_ids + the doc source rows + the acct
   config the manifests read (a_asset_acct, a_asset_change, a_depreciation_exp, c_project_acct, doc headers,
   c_validcombination additions) into `build/erp/oracle/post_b3_fixture.json` (TEXT fixture, committed — the
   DB-binary ban holds), exactly the extract_fact_acct.sh column shape (integer-cents on read).
**Execution findings (run 1-5, 2026-07-17, all in `build/erp/generate_post_oracle.log` history):**
- Tycho subset reactor needs the workspace-bundle dep closure + `org.apache.ecs` (a reactor wrapper module) —
  24 modules total, ~4 min/cycle; offline (-o) fails on p2 metadata, online resolves from cache.
- Scratch IDEMPIERE_HOME needs `utils/getVar.sh` + `.idpass` + `idempiereEnv.properties` (Ini.getVar reads the
  DB password through them — CConnection.java:65).
- **Dictionary-vs-code skew (this seed vs release-13 X classes):** `A_Depreciation_Workfile.UseLifeYears[_F]/
  A_Asset_Life_Years[_F]` are Integer (ref 11) in this seed's AD dictionary but BigDecimal in the compiled
  X class → ClassCastException. Fixed by retyping 11→22 ON THE CLONE only (dict seed prep).
- This seed's PRODUCT id for "Asset Vehicle" is 200001 (the official test-DB DictionaryIDs say 200000 —
  the DB-snapshot-divergence landmine, again).
- `MWorkflow.runDocumentActionWorkflow` hits an AD_WF_Process optimistic-lock failure on the SECOND in-trx
  run → switched to engine-direct `processIt(CO)` + explicit `DocManager.postDocument` (the same real posting
  entrypoint postImmediate/AcctProcessor call) — vendor-equivalent, deterministic.
- NOT-NULL-no-default columns this seed enforces beyond X defaults: entry DateDoc/IsApproved; transfer
  A_Period_Start/End + A_Transfer_Balance_IS (all unread by prepare/complete/createFacts).
**Oracle test file:** canonical copy `scripts/logic_oracle/PostingOracleTest.java` (bim-compiler); the generate
script rsyncs it into `org.idempiere.test/src/org/idempiere/test/oracle/` for the run (foreign checkout kept
clean otherwise). §W-3 diff: poc_post_b3.js §B3-GEN/§B3-POST materializes the JSON fixture into in-memory
sqlite, runs the extended `doc_poster.derivePostings` per class per schema, integer cents, maxDiff=0c.

# DONE — 2026-07-17, Opus lane (WORK-TO-ZERO). Every claim = a § line in `build/erp/poc_post_b3.log`
# (exit 0, log READ). Run: `bash build/erp/run_witness.sh scripts/poc_post_b3.js`.

- **§W-1 triage COMPLETE — all 8 classes classified from EXTRACTED facts** — `§B3-ORACLE` 8 lines: every
  target `fact_acct_rows=0 src_docs=0` (live PG + captured `glassbowl_data.db`, 300 rows none for these ids);
  `§B3-TRIAGE` 8 lines with per-class `createFacts` citation. Verdict: `🟢 triage: 2 ∅-by-design + 6 ⛔
  n/a-in-seed — design=2 seed-blocked=6`.
- **2 ∅-BY-DESIGN (✅ CORRECT, structurally proven)** — C_BankTransfer (200246), C_DepositBatch (200056):
  NO `Doc_<Class>` in `org.compiere.acct.Doc.get` factory (grep = no case) → posting is not in their
  contract → oracle=∅ is the RIGHT answer. `§B3-DERIVE … lines=0 basis=none` for both.
- **6 G-seed → ASSIGNED TO FABLE 5** — C_ProjectIssue + A_Asset_Addition/Disposed/Reval/Transfer +
  A_Depreciation_Entry: poster EXISTS, manifest source-parsed + line-cited (`§B3-TRIAGE`), but **0 source
  documents**. Per the USER RULING (2026-07-17) these are generatable via GardenWorld-model seed prep (a
  usability/PoC seed, NOT invention) → the Fable 5 session runs §W-2→§W-5 (prepare seed, drive the REAL
  compiled `Doc_<Class>` on scratch PG, capture fact_acct = oracle, diff `derivePostings` to `maxDiff=0c`).
  `§B3-ASSIGN` records all 6 + the hand-off. Only HAND-AUTHORING facts stays banned.
- **DOCTRINE CORRECTED** — my first pass read these 6 as "⛛ n/a-in-seed, prime-rule terminal" by over-applying
  the ISOMORPH-tail rule. The USER RULING overturned that: seed prep along the GardenWorld model is legitimate;
  the oracle is still the real poster's output. Card correction block + §W-2 updated accordingly.
- **§FALSIFIERS load-bearing (2)** — `§B3-FALSIFIER discriminates: busiest ad_table_id=319 rows=96 (>0) vs
  B-3 ids all 0` (the fact_acct=0 check is NOT vacuous) + `§B3-FALSIFIER live-verb C_Order=104 lines=3
  basis=invoice` (derivePostings IS live → the ∅ for B-3 classes is class-specific, not a dead verb).
- **HAND-OFF to Fable 5 (the open work)** — the 6 G-seed classes are NOT done; they are SCOPED + assigned.
  A Fable session prepares GardenWorld-model seed docs + `scripts/generate_post_oracle.sh` +
  `scripts/logic_oracle/PostingOracle.java` + extends `doc_poster` with the per-class manifest, per §W-2→§W-5.
  This triage supplies its starting map (arms, createFacts citations, verb + oracle-bundle locations).
- **§W-6 bank** — witness `scripts/poc_post_b3.js` committed; card DONE appendix here (on-disk, prompts/
  gitignored). Coverage-matrix / HARDEN / PROGRESS rows returned as data for the single Phase-3 writer, not
  cross-written from this card. ⏸ PUSH PAUSE honoured: committed locally, NOT pushed, no PR.
- **Regression note** — no engine/extract edit was made (triage + new witness only), so the existing bundle
  is untouched by construction; `glassbowl_data.db` read read-only, 300 rows intact.

# DONE — 2026-07-17, FABLE 5 lane (§W-2→§W-6 executed; the hand-off above is CLOSED). Every claim = a
# § line in `build/erp/poc_post_b3.log` (exit 0, READ) or `build/erp/generate_post_oracle.log` (GEN_EXIT=0, READ).
# Re-run: `bash scripts/generate_post_oracle.sh` (regenerates the oracle) then
#         `bash build/erp/run_witness.sh scripts/poc_post_b3.js` (the diff; also passes on the committed fixture alone).

- **§W-2 ORACLE GENERATED — real compiled posters, OSGi-hosted, scratch clone** — `scripts/generate_post_oracle.sh`:
  clone `idempiere_test`→`idempiere_b3` (`§GEN scratch=idempiere_b3 cloned`), `scripts/logic_oracle/
  PostingOracleTest.java` under the vendor's own `org.idempiere.test` tycho-surefire harness (resolves the
  POS_GAP_CLOSE §G-3 "no headless OSGi" landmine; vendor FixedAssetsTest = the addition recipe). All 9 seed docs
  completed AND posted by the REAL engine: `§ORACLE class=… docstatus=CO posted=Y` × 9 (3 Cap additions + the
  Exp-gate addition + entry + reval + transfer + disposal + project issue). Capture `§GEN-CAPTURE fact_acct
  b3-classes rows=34 ΣDRc=8136056 ΣCRc=8136056` → TEXT fixture `build/erp/oracle/post_b3_fixture.json` (24 tables,
  ~74KB, committed — the DB-binary ban holds); scratch DROPPED after capture.
- **§W-3 ALL 6 G-SEED CLASSES maxDiff=0c** — `§B3-POST class=<C> arm=G … maxDiff=0c oracle=compiled-Doc_<C>` for
  A_Asset_Addition (4 docs × 2 schemas) · A_Depreciation_Entry · A_Asset_Reval · A_Asset_Transfer ·
  A_Asset_Disposed · C_ProjectIssue (each 1 doc × 2 schemas), per document × schema × (natural-account, side),
  integer cents. `doc_poster.derivePostings` gained the 6 manifests (fa-addition/fa-depreciation/fa-reval/
  fa-transfer/fa-disposal/project-issue), each line-citing its Doc_*.java source; accounts resolve
  a_asset_acct/c_project_acct/a_asset_change/depexp combos → `c_validcombination.account_id` (the MAccount hop).
- **THE DIFF EARNED ITS KEEP (a real catch; cause CORRECTED 2026-07-18)** — first diff run: engine converted
  the addition's schema-200000 leg at 0.8006; the real poster posts 0.85. Initially attributed to
  `AD_Client_ID DESC` ordering ("tenant beats system") — WRONG: both USD→EUR rows are client 11; the live
  discriminator is `MConversionRate.getRate:251`'s **`IsActive='Y'`** clause (the 0.8006 twin is inactive).
  Caught the day after by poc_alloc_fx §FALSIFIER-B when a capture-side isactive filter vacuated its
  wrong-rate prop. Final state: the inactive row is CAPTURED (isactive column added), and `fxRate` carries
  the verbatim `:243-252` clauses (BETWEEN valid dates, IsActive, client/org scope + ordering). Also the
  Doc_AssetAddition currency finding stands: it is the ONLY B-3 poster that posts DOC-currency amounts
  (converted per schema) — the other five pass the schema's own currency.
- **§W-4 semantics arm** — no separate PostingOracle.java stdin-driver needed: the OSGi test harness IS the
  semantics arm (real Doc.getAccount/MAssetAcct/MConversionRate/MCost resolution ran inside the real engine).
  Named infra findings (in the card's §W-2 EXECUTION SPEC block): AD-dictionary-vs-code type skew (workfile
  uselife columns 11→22, clone-only), seed's product-id divergence (200001 not 200000), wf-engine optimistic-lock
  on 2nd in-trx run → engine-direct processIt + DocManager.postDocument, per-step commits for null-trx readers
  (Doc_AssetReval.java:105 / Doc_AssetDisposed.java:96).
- **§W-5 falsifiers load-bearing (2 new + 2 triage)** — `§B3-FALSIFIER gate-flip addition=1000000 lines=2→0`
  (A_CapvsExp Cap→Exp → ∅; the ORACLE side is the seed's own Exp addition: `§ORACLE expgate_addition=1000002
  fact_rows=0` — zero is CONFIG-derived, Doc_AssetAddition.java:67-72, the poc_morder_post move) ·
  `§B3-FALSIFIER scale-line depexp=1000000 oracleΣDR=55556c scaledΣDR=111112c` (the metric is load-bearing) ·
  the two §W-1 triage falsifiers unchanged. Vacuous-∅ guard: witness REFUSES a pass on an empty oracle
  (it fired for real on the untyped-fixture bug run, then on the typed fixture: 34 rows, non-vacuous).
- **§W-5 witness green + regressions green** — `🟢 W-POST-B3 PASS` exit 0; after the doc_poster/extract-side
  changes: `poc_post_harden` · `poc_factacct_doc` · `poc_doc_poster` · `poc_morder_post` all exit 0 and
  `test_report_fin` still balances 46574.97 over the same 300 rows (fixture is a SEPARATE additive file —
  glassbowl_data.db untouched).
- **§B3-SKIPS / honesty** — 2 ∅-by-design stay structurally closed (no Doc_ factory case); A_Depreciation_Entry
  NOT deferred to DepreciationPerf (that lane measured workfile-BUILD cost, never posted facts — cited, distinct);
  schema-200000 depreciation-entry side left unentered ON PURPOSE (exercises the other-schema ∅ branch, which the
  diff covers as 0==0 across the schema axis). Seed prep honestly labelled per the USER RULING (uselife 18 +
  MDI/FMCON methods, FDP doctype, reval-offset acct := disposal-revenue combo, dict retype on clone).
- **§W-6 banked (this card = its own single writer)** — `docs/internal/ERP_COVERAGE_MATRIX.md` +1 evidence row,
  ledger 43→**49**; `prompts/HARDEN_MATRIX.md` B-3 band ✅; `prompts/FABLE5_WORKFLOW_ORACLE.md` STOP-CONDITION
  pointer → "opened + banked"; `PROGRESS.md` archive line. Committed locally; **⏸ PUSH PAUSE honoured — NOT
  pushed, no PR.** Foreign checkout `~/idempiere-dev-setup/idempiere` left byte-identical to its pre-session
  state (test file rsynced in per-run and removed; target-platform patch reverted).
