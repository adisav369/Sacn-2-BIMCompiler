# 00r — Wire RouteExecutor to consume DISC BOMs for all disciplines

**Spec:** DISC_VALIDATION_DB_SRS.md §11 + §10.4.11
**Prereq:** 00q DONE — all DISC BOM lines populated, discipline column set on joint pieces
**Test Stones:** RM, SH — gate counts must not change, GEO DRIFT=0
**Scope:** Java only — RouteExecutor + new discipline Placers. No bypass removal yet.

You are a coder for bim-compiler. One bounded task.

## PRIME RULE

**EXTRACT OR COMPILE ONLY.** No invention. Replicate FireSuppressionPlacer pattern exactly.

## Read first

1. `PROGRESS.md` §Current State
2. `docs/DISC_VALIDATION_DB_SRS.md` §11 (archetype → piece-type map)
3. `DAGCompiler/.../library/FireSuppressionPlacer.java` — full file (the pattern to replicate)
4. `DAGCompiler/.../dsl/RouteExecutor.java` — full file (dispatch point)
5. `DAGCompiler/.../bom/walker/ShimMatcher.java` — full file (DO NOT remove yet, just understand)
6. `docs/TestArchitecture.md` §Anti-Drift before any changes
7. This prompt fully

## Witness claim

**W-DISC-WIRE:** After wiring:
- `RouteExecutor` dispatches to a Placer for each of ELEC, ACMV, CW, SP, GAS
- Each Placer reads `_import_joint_piece_types WHERE discipline=X`
- RM + SH: gate counts unchanged, GEO DRIFT=0
- ShimMatcher still present (bypass not yet removed — that is 00s)

## Pre-flight citation

Before each Placer class, add:
`// Implementing DISC_VALIDATION_DB_SRS §11.3 — Witness: W-DISC-WIRE`

## Task A — One Placer per discipline

For each of ELEC, ACMV, CW, SP, GAS:
1. Create `{Disc}Placer.java` in `DAGCompiler/.../library/`
2. Pattern: identical structure to `FireSuppressionPlacer.java`
3. Constructor takes `(Connection erpDb, String sourceBuilding)`
4. `place(BOMLine line, PlacementContext ctx)` method
5. Reads joint pieces: `SELECT * FROM _import_joint_piece_types WHERE discipline=? AND source_building=?`
6. Maps archetype names from §11.3 to the correct piece_type rows

## Task B — RouteExecutor dispatch

In `RouteExecutor.java`, add dispatch cases for ELEC, ACMV, CW, SP, GAS
following the existing FP dispatch pattern exactly.

## Task C — Compile + run RM + SH

```bash
mvn compile -q
./scripts/run_RosettaStones.sh classify_rm.yaml classify_sh.yaml
```

Check:
- Gate counts unchanged (ShimMatcher still active — counts should be same as before)
- No compilation errors on new Placer classes
- GEO DRIFT=0 in logs

## Scope boundary

- New Placer Java files only + RouteExecutor dispatch only
- Do NOT remove ShimMatcher — that is 00s
- Do NOT remove TE_BOM.db MEP sub-BOMs — that is 00t
- Do NOT touch migration SQL files
- Do NOT touch *_BOM.db files

## Report back (append after # DONE)

1. Which Placers created? (list class names)
2. RouteExecutor dispatch: how many new cases added?
3. RM gate count before vs after?
4. SH gate count before vs after?
5. GEO DRIFT: still 0?
6. Any joint piece rows not matched by new Placers?

## Commit

`[S104-DISC] 00r: ELEC/ACMV/CW/SP/GAS Placers + RouteExecutor dispatch (W-DISC-WIRE)`

---

# DONE

