# 00s — Remove ShimMatcher bypass for RM. Hard fail. GEO proof.

**Spec:** DISC_VALIDATION_DB_SRS.md §6.12.1 (Compilation Isolation Invariant)
**Prereq:** 00r DONE — all discipline Placers wired, RM gate counts stable
**Test Stones:** RM — must reach 8/8 via DISC BOM path alone
**Scope:** Remove ShimMatcher MEP path. Hard fail if DISC BOM path incomplete.

You are a coder for bim-compiler. One bounded task.

## PRIME RULE

**EXTRACT OR COMPILE ONLY.** No fallbacks. If it doesn't compile through DISC BOM, it fails.

## Read first

1. `PROGRESS.md` §Current State
2. `docs/DISC_VALIDATION_DB_SRS.md` §6.12.1
3. `DAGCompiler/.../bom/walker/ShimMatcher.java` — full file
4. `DAGCompiler/.../dsl/RouteExecutor.java` — current dispatch after 00r
5. `docs/TestArchitecture.md` §Anti-Drift
6. This prompt fully

## Witness claim

**W-DISC-RM:** After removal:
- `ShimMatcher.java` MEP bypass path is deleted (file may remain for non-MEP uses if any)
- RM: 8/8 gates — MEP elements compiled via DISC BOM Placers only
- RM GEO DRIFT = 0, element count unchanged
- SH: 8/8 — no regression (SH has no MEP, should be unaffected)

## Pre-flight citation

`// Removing ShimMatcher MEP bypass — DISC_VALIDATION_DB_SRS §6.12.1 Compilation Isolation Invariant`

## Task A — Audit ShimMatcher call sites

Find all callers of ShimMatcher in the compiler:
```bash
grep -rn "ShimMatcher\|shimMatcher\|shim_matcher" \
  DAGCompiler/src/main/java/ --include="*.java"
```

Categorise each call: MEP path (remove) vs any non-MEP path (keep).
If ShimMatcher is used ONLY for MEP: delete the class.
If used for non-MEP also: remove only the MEP dispatch, leave rest.

## Task B — Add hard fail

In RouteExecutor (or wherever ShimMatcher was called), replace the ShimMatcher
call with a hard throw:
```java
throw new IllegalStateException(
    "MEP element reached ShimMatcher bypass — DISC BOM path incomplete for: "
    + element.getIfcClass() + " / " + element.getElementName());
```

This ensures any element that slips through the new Placers fails loudly.

## Task C — Compile + run RM + SH

```bash
mvn compile -q
./scripts/run_RosettaStones.sh classify_rm.yaml classify_sh.yaml
```

If RM drops below 8/8: the Placers from 00r have a gap. Diagnose from the
IllegalStateException message — which element type is unmatched.
Fix the Placer gap (add missing piece_type handling). Do NOT restore ShimMatcher.

## Scope boundary

- ShimMatcher MEP path only
- Do NOT touch TE_BOM.db MEP sub-BOMs — that is 00t
- Do NOT touch other buildings' compilation paths
- Do NOT weaken any gate assertion

## Report back (append after # DONE)

1. ShimMatcher: deleted entirely or MEP path only removed?
2. Any IllegalStateException thrown during RM compile? Which element types?
3. RM gate count: 8/8?
4. RM GEO DRIFT: 0?
5. SH: still 8/8?

## Commit

`[S104-DISC] 00s: remove ShimMatcher MEP bypass — RM via DISC BOM only (W-DISC-RM)`

---

# DONE

