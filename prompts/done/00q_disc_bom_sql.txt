# 00q — DISC BOM SQL: populate ELEC/ACMV/CW/SP/GAS in ERP.db + AD_Org_ID on joint pieces

**Spec:** DISC_VALIDATION_DB_SRS.md §11 (written by 00p)
**Prereq:** 00p DONE — §11.2 piece-type map clean (no G2 gap), §11.3 archetype pattern confirmed
**Test Stones:** RM, TE — gate counts must not change
**Scope:** SQL migration only. No Java.

You are a coder for bim-compiler. One bounded task.

## PRIME RULE

**EXTRACT OR COMPILE ONLY.** No invention. Read §11 findings, replicate the FP pattern exactly.

## Read first

1. `PROGRESS.md` §Current State
2. `docs/DISC_VALIDATION_DB_SRS.md` §11 (00p findings — the ONLY source for archetype names)
3. `docs/DISC_VALIDATION_DB_SRS.md` §6.2 (Discipline as sub-BOM)
4. `migration/` — find the highest-numbered migration, understand naming convention
5. `library/ERP.db` — query current state:
   ```sql
   SELECT b.Name, b.AD_Org_ID, COUNT(bl.M_BOM_Line_ID) lines
   FROM m_bom b LEFT JOIN m_bom_line bl ON bl.M_BOM_ID=b.M_BOM_ID
   WHERE b.AD_Org_ID > 2 GROUP BY b.M_BOM_ID;
   PRAGMA table_info(_import_joint_piece_types);
   ```
6. This prompt fully

## Witness claim

**W-DISC-SQL:** After migration:
- All 6 DISC BOMs have ≥ 3 ROUTE lines (ELEC, ACMV, CW, SP, GAS following FP pattern)
- `_import_joint_piece_types` has a `discipline` (or `AD_Org_ID`) column
- RM + TE gate counts unchanged (no compilation run yet — data layer only)

## Task A — Add discipline column to _import_joint_piece_types

If 00p found G1 (column missing), add it in the migration:
```sql
ALTER TABLE _import_joint_piece_types ADD COLUMN discipline TEXT;
```
Then UPDATE each row using the piece-type → discipline map from §11.2.
Use AD_Org_ID values: FP=3, ELEC=4, ACMV=5, CW=6, SP=7, GAS=8.

If G1 was not found (column already present), verify values are set correctly.

## Task B — Populate abstract DISC BOM lines

For each empty DISC BOM (ELEC, ACMV, CW, SP, GAS), INSERT m_bom_line rows
following the FP pattern exactly:
- component_type = 'MAKE', verb_ref = archetype name from §11.3
- child_product_id = 0, dx=0, dy=0, dz=0, qty=1
- sequence = 10, 20, 30, ... per archetype

Archetype names MUST come from §11.3. Do not invent names.
If §11.3 has fewer than 3 archetypes for a discipline, use what exists.

## Scope boundary

- Migration file only: `migration/DV_RM_rules.sql` or new file — append only
- Do NOT run the migration manually — the pipeline applies it
- Do NOT touch Java source
- Do NOT touch *_BOM.db files
- Do NOT compile or run Rosetta Stones (compilation path not yet wired)

## Verification

```sql
-- Verify after migration applied:
SELECT b.Name, b.AD_Org_ID, COUNT(bl.M_BOM_Line_ID) lines,
       GROUP_CONCAT(bl.verb_ref, ' | ') archetypes
FROM m_bom b LEFT JOIN m_bom_line bl ON bl.M_BOM_ID=b.M_BOM_ID
WHERE b.AD_Org_ID > 2 GROUP BY b.M_BOM_ID;

SELECT discipline, COUNT(*) FROM _import_joint_piece_types GROUP BY discipline;
```

Expected: all 6 DISC BOMs ≥ 3 lines, all joint piece rows have discipline set.

## Report back (append after # DONE)

1. G1 resolved? (discipline column added/verified)
2. Lines added per discipline (ELEC/ACMV/CW/SP/GAS)
3. Archetype names used — exactly as in §11.3?
4. Any joint piece rows with NULL discipline after UPDATE?

## Commit

`[S104-DISC] 00q: ERP.db DISC BOM lines + joint piece discipline column (W-DISC-SQL)`

---

# DONE

